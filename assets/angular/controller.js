//app_name.controller-keyword('naming the controller', [array of services], function callback ({}))



app.controller('home', ['$scope', function ($scope) {
  $scope.hello="world";
}]);

app.controller('createBook', ['$scope', function ($scope) {
  $scope.hello="world";
}]);

app.controller('sign-in', ['$scope', function ($scope) {
  $scope.hello="world";
}]);

app.controller('sign-up', ['$scope', function ($scope) {
  $scope.hello="world";
}]);

app.controller('contact', ['$scope', function ($scope) {
  $scope.hello="world";
}]);

app.controller('checkout', ['$scope', function ($scope) {
  $scope.hello="world";
}]);

app.controller('thanks', ['$scope', function ($scope) {
  $scope.hello="world";
}]);

app.controller('cart', ['$scope', function ($scope) {
  $scope.hello="world";
}]);

app.controller('account', ['$scope', function ($scope) {
  $scope.hello="world";
}]);
